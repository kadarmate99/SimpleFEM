<UserControl 
        x:Class="SimpleFEM.UI.Views.LaunchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleFEM.UI.Views"
        mc:Ignorable="d"
        Height="300" Width="450"
        d:DesignHeight="150" d:DesignWidth="225" d:FontSize="16"
        >
    <Grid>
        <Grid.RowDefinitions>
            <!-- Spacer -->
            <RowDefinition Height="*"/>
            <!-- Buttons -->
            <RowDefinition Height="Auto"/>
            <!-- Spacer -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">

            <Button 
                        Content="New" 
                        Command="{Binding NewModelFileCommand}"
                        CommandParameter="Line"
                        Style="{StaticResource ToolbarButtonStyle}"/>
            <Button 
                        Content="Open" 
                        Command="{Binding OpenModelFileCommand}"
                        CommandParameter="Node"
                        Style="{StaticResource ToolbarButtonStyle}"/>

        </StackPanel>
    </Grid>
</UserControl>
